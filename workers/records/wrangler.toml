name = "doctorcare-records"
main = "src/index.ts"
compatibility_date = "2024-09-18"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Secrets Store Provisioning & Exclusive Binding:
# Key-Encryption Key (KEK) named kek-2026-09 bound EXCLUSIVELY to this Worker.
# Neither api nor notify worker has access to this store or key.
[[secrets_store_secrets]]
binding = "KEK_2026_09"
store_id = "d88f06ff053b41eeb43845d9007659d7"
secret_name = "kek-2026-09"

# Cloudflare D1 Isolated Medical Records Database (strictly PHI / ciphertext only)
# Bound EXCLUSIVELY to this records Worker. Neither api nor notify has access.
[[d1_databases]]
binding = "RECORDS_DB"
database_name = "doctorcare-records-db"
database_id = "aaea3723-c27a-4a3a-bce7-07f37ae39f55"

[vars]
ENVIRONMENT = "production"


# Cloudflare R2 Buckets (Enable R2 in Cloudflare dashboard to activate):
# [[r2_buckets]]
# binding = "PATIENT_FILES_BUCKET"
# bucket_name = "doctorcare-patient-files"
# 
# [[r2_buckets]]
# binding = "AUDIT_VAULT_BUCKET"
# bucket_name = "doctorcare-audit-vault"
